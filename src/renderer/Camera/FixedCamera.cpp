#include "fmepch.h"
#include "FixedCamera.h"
#include "glm/gtc/matrix_transform.hpp"

namespace FMEditor {
	//FixedCamera::FixedCamera(glm::vec3 position, glm::vec3 direction)
	//	: Camera(position, direction), m_FocusPoint(glm::vec3(0, 0, 0)) {}

	//void FixedCamera::SetProjectionMatrix(float fov, float aspectRatio, float nearPlane, float farPlane)
	//{
	//	m_Projection = glm::perspective(
	//		glm::radians(fov),
	//		aspectRatio,
	//		nearPlane,
	//		farPlane
	//	);
	//}
}