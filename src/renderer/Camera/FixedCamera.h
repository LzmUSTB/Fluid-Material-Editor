#pragma once
#include "Camera.h"

namespace FMEditor {

	//class FixedCamera : public Camera
	//{
	//public:
		//FixedCamera(glm::vec3 position,glm::vec3 direction);
	//private:
		//glm::vec3 m_FocusPoint;

		// Inherited via Camera
		//void SetProjectionMatrix(float fov, float aspectRatio, float nearPlane, float farPlane) override;
	//};

}

